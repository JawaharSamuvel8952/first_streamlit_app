version: 2

semantic_models:
  - name: customer_details
    model: ref('dim_customers')

    entities:
      - name: customer
        expr: customer_id
        type: primary

    dimensions:
      - name: country
        type: categorical
